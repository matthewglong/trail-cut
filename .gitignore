# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Rust / Tauri
src-tauri/target
src-tauri/gen
# Renderer binding artifacts (patched @maplibre/maplibre-gl-native) are
# staged at build time by src-tauri/sidecars/renderer/build.mjs via
# native/ensure-binding.mjs, not committed.
src-tauri/binaries/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

.playwright-mcp
# Color-pipeline teaching lab (rendered frames, swatches, montages)
pipeline-visuals/

# Quarantined cruft — preserved on disk, never read (deny-listed in .claude/settings.json)
attic/
